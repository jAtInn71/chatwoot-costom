# Execution Plan — chatwoot-custom-master

Short index of current work. Detailed plans live in `.antigravity/details/plans/`.

---

## Now (active work)

- _No active plan yet — to be filled when next task starts._

## Next (queued)

- Investigate purpose of empty `custom-widget/` directory at repo root.
- Document ElevenLabs API key source (where does the runtime key come from?).
- Confirm CI/CD setup (or absence) and document.

## Later (someday/maybe)

- Migration path for upstream Chatwoot upgrades.
- Test coverage strategy for `custom/` code.

---

For machine-readable task state, see `agents/state/tasks.json`. For the human-facing task board, see `.antigravity/TASKS.md`.
