# Task Board (Human View) — chatwoot-custom-master

Canonical machine-readable state: `agents/state/tasks.json`.

---

## Intake Queue (not yet planned)

- [ ] Verify what the empty `custom-widget/` directory at repo root is for.
- [ ] Document where ElevenLabs API key is injected at runtime (`apiKey: ''` is empty in `appConfig.js` — find the real source).
- [ ] Audit `test/` directory — what's tested, what's missing.
- [ ] Document CI/CD pipeline (or note its absence).

## In Progress

- _None._

## Done (recent)

- Initialized Dev Harness Core overlay (this folder structure).
- Filled `.antigravity/memory.md`, `RULES.md`, `PLAN.md`, `TASKS.md` with project-specific content.

---

When you start a task, move it to **In Progress** and mirror it in `agents/state/tasks.json`.
When you finish, move to **Done** with a one-line summary.
